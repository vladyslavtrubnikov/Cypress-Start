{
  "name": "cypress-start",
  "version": "1.0.0",
  "description": "start to do with cypress",
  "main": "index.js",
  
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:user1": "cross-env CYPRESS_USER=USER1 npx cypress run",
    "test:user2": "cross-env CYPRESS_USER=USER2 npx cypress run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^14.0.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.7",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
